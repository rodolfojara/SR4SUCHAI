#include <stdio.h>
#include "RobustSR.h"
// Sign: Entrega 1 si el valor es mayor a cero, cero si vale cero, y -1 si es negativo
/*
Inputs:
X - A matrix with values

Output:
 The sign of values of inputs
*/
TipoImagenSR sign(TipoImagenSR X)
{
 TipoImagenSR Y;
/*
Y = (x > 0) ? 1 : ((x<0) ? -1 : 0)
*/
	return Y;
}
